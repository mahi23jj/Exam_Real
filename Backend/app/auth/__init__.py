"""Auth0 authentication module.

This module is the single integration point with Auth0.
The rest of the application communicates only through the
public dependencies exposed here (get_current_user, get_current_user_optional).
"""
